# Reconciled, verified-working dependency set for the Cho2017 benchmark.
#
# These are the exact versions installed and validated for this project (the
# import, model-shape and unit tests all pass against them). They were resolved
# on a Python 3.13 host; all also provide wheels for the Python 3.11 reference
# interpreter (see environment.yml).
#
# torch is pinned without the local CUDA tag. The host used a CUDA build
# (torch 2.6.0+cu124); for GPU support install the matching wheel from the
# official PyTorch index, e.g.:
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# The CPU wheel (plain `torch==2.6.0`) is sufficient for everything here.
#
# ATCNet (default model) requires braindecode >= 1.1; 1.5.2 is used here.

numpy==2.2.6
scipy==1.16.3
pandas==2.3.3
scikit-learn==1.8.0
matplotlib==3.10.8
seaborn==0.13.2
mne==1.12.1
moabb==1.5.0
torch==2.6.0
braindecode==1.5.2
pyyaml==6.0.3
tqdm==4.67.1
joblib==1.5.3
pyarrow==24.0.0
requests==2.32.5
jupyter==1.1.1
ipykernel==7.1.0
pytest==8.4.2
