# Reproducible reference pins for the Cho2017 Motor Imagery Benchmark.
#
# These versions are the canonical, mutually-compatible reference set validated
# for Python 3.11 (see environment.yml for an equivalent conda specification).
#
# NOTE ON NEWER PYTHON (e.g. 3.13): some of these exact builds (notably torch
# and numpy) may not ship wheels for the running interpreter. In that case pip
# will need compatible newer builds. The *actually installed* versions on any
# machine are always recorded next to every experiment via
# `reproducibility.collect_environment()` (saved under results/*/), so the real
# provenance is never lost even if these pins are relaxed at install time.
#
# ATCNet requires braindecode >= 1.1 (the default model implementation); do not
# downgrade braindecode below a release that exposes `braindecode.models.ATCNet`.

numpy==2.0.2
scipy==1.13.1
pandas==2.2.3
scikit-learn==1.5.2
matplotlib==3.9.2
mne==1.8.0
moabb==1.1.0
torch==2.4.1
braindecode==1.5.1
pyyaml==6.0.2
tqdm==4.66.5
joblib==1.4.2
pyarrow==17.0.0
requests==2.32.3
jupyter==1.1.1
ipykernel==6.29.5
pytest==8.3.3
